SELECT DISTINCT * FROM users; 
#traer todos los datos de la tabla sin repetir los datos

SELECT DISTINCT age FROM users; 
#traer solo una columna de la tabla sin repetir los datos

SELECT users_id, name FROM users;
#traer solo dos columnas de la tabla
