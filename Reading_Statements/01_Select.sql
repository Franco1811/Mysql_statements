SELECT * FROM users;
#traer todos los datos de la tabla

SELECT name FROM users;
#forma de traer solo una columna de la tabla

SELECT users_id, name FROM users;
#traer solo dos columnas de la tabla

#palabras reservadas en mayusculas para que sea mas claro que es una palabra propia del lenguaje
#si tengo varias lineas solo la selecciono y y le doy al rayito para que solo se ejecute esa